source "https://rubygems.org"

# group for development, test
group :development, :test do
  # This is here because gemspec doesn't support require: false
  gem 'coveralls', :require => false
end

gemspec
